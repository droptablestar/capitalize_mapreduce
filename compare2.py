import sysresults = []truths = []#Loads the truth file data into an internal listdef loadTruthData():
	i = 0	with open(sys.argv[1]) as f:		for line in f:			truths.append(line)			i = i + 1#Loads the result file data into an internal listdef loadResultData():	i = 0	with open(sys.argv[2]) as f:		for line in f:			results.append(line)			i = i + 1def printResults():	for result in results:		print '%s' % result	print 'len : %s' % len(results)#compare the truth file agains the results obtained#and print the numbers of words predicted wrongdef main():	loadTruthData()	loadResultData()	count = 0	for i in range(len(truths)):		truth = truths[i].strip()		result = results[i].strip()		i = i + 1		if len(truth) == 0 or len(result) == 0:			count = count		else:			if result[0] != truth[0]:				print result[0],truth[0]				count = count + 1 	print 'count: %s ' % countif __name__ == '__main__':	main()